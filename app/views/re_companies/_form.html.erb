<%= form_with(model: re_company, local: true) do |form| %>
  <% if re_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(re_company.errors.count, "error") %> prohibited this re_company from being saved:</h2>

      <ul>
      <% re_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
